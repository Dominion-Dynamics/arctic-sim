<?xml version="1.0"?>
<model>
  <name>rover_front_camera</name>
  <version>1.0</version>
  <sdf version="1.6">model.sdf</sdf>

  <author>
    <name>Nick</name>
    <email>nickaleks@ddc2.com</email>
  </author>

  <description>
    Forward EO camera for rover_core, streamed as MJPEG by CameraStreamPlugin.
    A separate model so make_world.py's camera rewrite cannot reach the rover's
    IMU — see the note in model.sdf.
  </description>
</model>
